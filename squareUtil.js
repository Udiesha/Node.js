const add = (num1, num2) => {
    return num1 + num2;
}

const multiple =  (num1, num2) => {
   return num1 * num2;
}

module.exports = {
   squareFn : (number) => {
       return number * number;
   },    
   data : "test",
   dataNew : true,
   add,
   multiple

} 
